@{
    ViewData["Title"] = "Panel Empleado";
}
<div class="container py-4">
    <h2 class="mb-3">Panel de Empleado</h2>

    <div class="mb-3">
        <a class="btn btn-primary" href="@Url.Action("Index","Products")">
            Gestión de productos
        </a>
    </div>

    <a class="btn btn-outline-secondary" href="@Url.Content("~/")">← Regresar a la tienda</a>
</div>
